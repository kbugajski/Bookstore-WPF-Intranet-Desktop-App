<Window x:Class="BookstoreApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:BookstoreApp.ViewResources"
        xmlns:local="clr-namespace:BookstoreApp.Views"
        xmlns:ViewModels="clr-namespace:BookstoreApp.ViewModels"
        
        mc:Ignorable="d"
        Title="Bookstore" Height="500" Width="800">

    <Window.Resources>
        <ResourceDictionary Source="MainWindowResources.xaml"/>
    </Window.Resources>

    <Window.DataContext>
        <ViewModels:MainWindowViewModel/>
    </Window.DataContext>

    <Grid x:Name="grid" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>


        <Menu Grid.Row="0" Grid.Column="0">
            <MenuItem Header="Books" Command="{Binding OpenBooksViewCommand}" HorizontalAlignment="Center" MinWidth="50" Height="26" Background="LightBlue" Margin="2, 2, 2, 2"/>
            <MenuItem Header="Authors" Command="{Binding OpenAuthorsViewCommand}" HorizontalAlignment="Center" MinWidth="50" Height="26" Background="LightBlue" Margin="2, 2, 2, 2"/>
            <MenuItem Header="Add Book" Command="{Binding OpenAddBookViewCommand}" HorizontalAlignment="Center" MinWidth="50" Height="26" Background="CornflowerBlue" Margin="2, 2, 2, 2"/>
            <MenuItem Header="Add Author" Command="{Binding OpenAddAuthorViewCommand}" HorizontalAlignment="Center" MinWidth="50" Height="26" Background="CornflowerBlue" Margin="2, 2, 2, 2"/>
        </Menu>

        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Margin="4">
                <HeaderedContentControl
                    Header="Commands"
                    Background="Aqua"
                    Content="{Binding Commands}"
                    ContentTemplate="{StaticResource CommandsTemplate}"
                    Style="{StaticResource MainHCCStyle}"
                    />

            </Border>
            <Border Grid.Column="1" Padding="2, 2, 2, 2">
                <HeaderedContentControl
                    Header="Workspace"
                    Background="Aqua"
                    Content="{Binding Workspaces}"
                    ContentTemplate="{StaticResource WorkspacesTemplate}"
                    Style="{StaticResource MainHCCStyle}"
                    />

            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Grid.Column="0">
            <StatusBarItem>
                <TextBlock><Run Text="BookStore App"/></TextBlock>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock><Run Text="BOOKSERS"/></TextBlock>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
